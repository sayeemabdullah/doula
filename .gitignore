# doula.skill IS tracked in this repo (and also attached to every release).
# Rebuild and commit it whenever doula/ changes:
#   rm -f doula.skill && zip -r -X doula.skill doula -x '.*' '*/.*'
.DS_Store
